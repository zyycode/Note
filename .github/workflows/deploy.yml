name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    name: Build
    runs-on: ubuntu-18.04

    steps:
    - name: yarn install
      run:
        yarn install

  deploy:
    steps:
      name: yarn deploy
      run:
        yarn deploy